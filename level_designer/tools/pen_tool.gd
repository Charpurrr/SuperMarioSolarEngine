class_name PolygonTool
extends Tool
## Drawing and creating polygons.

## The internal mode the pen tool is in. `CREATE` is for creating new shapes, `EDIT` is for editing existing shapes
enum Mode {CREATE, EDIT}
